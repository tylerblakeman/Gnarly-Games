        <div class="col-lg-3 col-sm-6">
          <div class="card" style="height: 350px">
            <div class="card-body d-flex flex-column">
              <img src="" alt="">
              <h5 class="card-title">{{title}}</h5>
              <div class="scrollbar scrollbar-primary">
                <div class="card-text details-{{gameId}} overflow-auto" style="height: 150px;">{{summary}}</div>
              </div>
                <button type="button" id="{{gameId}}" class="save-game filterBtns btn m-2 mt-auto">Save</button>
            </div>
          </div>
        </div>

